/**
 * Test case for colorConstants.
 * Runs with mocha.
 */
'use strict'

const colorConstants = require('../lib/constants/color_constants.js')
const assert = require('assert')
const co = require('co')

describe('color-constants', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Color constants', () => co(function * () {

  }))
})

/* global describe, before, after, it */
