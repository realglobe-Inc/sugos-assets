/**
 * Test case for fontConstants.
 * Runs with mocha.
 */
'use strict'

const fontConstants = require('../lib/constants/font_constants.js')
const assert = require('assert')
const co = require('co')

describe('font-constants', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Font constants', () => co(function * () {

  }))
})

/* global describe, before, after, it */
