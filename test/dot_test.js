/**
 * Test case for dot.
 * Runs with mocha.
 */
'use strict'

const dot = require('../lib/dot.js')
const assert = require('assert')
const co = require('co')

describe('dot', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Dot', () => co(function * () {

  }))
})

/* global describe, before, after, it */
