/**
 * Test case for fsHelper.
 * Runs with mocha.
 */
'use strict'

const fsHelper = require('../lib/helpers/fs_helper.js')
const assert = require('assert')
const co = require('co')

describe('fs-helper', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Fs helper', () => co(function * () {

  }))
})

/* global describe, before, after, it */
