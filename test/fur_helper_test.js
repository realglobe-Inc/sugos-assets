/**
 * Test case for furHelper.
 * Runs with mocha.
 */
'use strict'

const furHelper = require('../lib/helpers/fur_helper.js')
const assert = require('assert')
const co = require('co')

describe('fur-helper', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Fur helper', () => co(function * () {

  }))
})

/* global describe, before, after, it */
